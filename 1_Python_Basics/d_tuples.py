"""
Tuples:
difference between tuples and list is that, tuples are immutable (cannot be modified)
tuples are defined in round brackets ()
"""

tuple1 = ("math", "biology", "computer")
print(tuple1)

# we cannot modify tuples, but other than that we can do everything else, like search through it, access certain elements, etc